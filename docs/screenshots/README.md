Drop screenshots here using these filenames — the main README references them directly:

- `landing.png`
- `feed.png`
- `problem-detail.png`
- `onboarding.png`
- `building.png`
- `project-analytics.png`

Any image format works as long as the filename (minus extension) matches; update the extension
in the main README if you're not using `.png`.
